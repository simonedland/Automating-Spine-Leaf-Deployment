#the function that initialises the file and creates the induvidual spaces for eatch node command counter
def initFile(node):
    return 2



#countFile = open("commandCount.txt", "w+")
#lines=countFile.readlines()
#print(lines)
#if len(lines)==0:
#    countFile.write(node.host.hostname + " " + "0" + "\n")
#for x in lines:
#    if node.host.hostname in x:
#        print("found")
#    else:
#        countFile.write(node.host.hostname + " " + "0" + "\n")
#        print("not found")
##countFile.writelines(f"{node.host.hostname}")
#countFile.close()